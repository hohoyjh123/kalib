<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <import type="android.view.View"/>
        <import type="android.graphics.Typeface"/>
        <variable
            name="viewModel"
            type="com.yesjnet.gwanak.ui.main.LoginViewModel" />
        <variable
            name="item"
            type="com.yesjnet.gwanak.data.model.Family" />
        <variable
            name="pos"
            type="Integer" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clBarcode"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="18dp"
        tools:background="@color/color_008600">
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/clName"
            android:layout_width="match_parent"
            android:layout_height="30dp"
            android:layout_marginTop="15dp"
            app:layout_constraintTop_toTopOf="parent">
            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="8dp"
                android:paddingVertical="2dp"
                android:textSize="12dp"
                android:textColor="@color/white"
                bindCardStatus="@{item}"
                position="@{pos}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toStartOf="@id/tvName"
                app:layout_constraintHorizontal_chainStyle="packed"
                tools:text="@string/my"
                tools:background="@color/primary_color"/>
            <TextView
                android:id="@+id/tvName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textColor="@color/color_333333"
                android:textSize="14dp"
                bindCardName="@{item}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toEndOf="@id/tvStatus"
                app:layout_constraintEnd_toEndOf="parent"
                tools:text="@string/login_after_mobile_card_format"/>
        </androidx.constraintlayout.widget.ConstraintLayout>
        <!-- 바코드 영역 -->
        <ImageView
            android:id="@+id/ivBarcode"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_marginHorizontal="56dp"
            android:layout_marginTop="18dp"
            android:scaleType="fitXY"
            android:onClick="@{() -> viewModel.onClickBarcode(item)}"
            app:bindCreateBarcode="@{item.familyUserNo}"
            app:layout_constraintTop_toBottomOf="@id/clName"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>
        <TextView
            android:id="@+id/tvLoanNo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:textColor="@color/color_333333"
            android:textSize="18dp"
            android:fontFamily="@font/style_regular"
            android:text="@{item.familyUserNo}"
            app:layout_constraintTop_toBottomOf="@id/ivBarcode"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="1234123412391239"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

